<section class="steps-section">
  <div class="container">
    <div class="steps-intro">
      <h2>Asistencia digital en tres pasos simples</h2>
      <p>Diseñado minuciosamente para ser claro, directo y sin pérdidas.</p>
    </div>

    <div class="steps-grid">
      <div class="step-card">
        <div class="step-number">01</div>
        <h3>Escribe tu duda</h3>
        <p>Introduce una pregunta tecnológica corta sobre lo que necesitas resolver.</p>
      </div>

      <div class="step-card">
        <div class="step-number">02</div>
        <h3>IA instantánea</h3>
        <p>Nuestro sistema genera al momento un tutorial paso a paso con texto simple e explicativo.</p>
      </div>

      <div class="step-card">
        <div class="step-number">03</div>
        <h3>Conexión voluntaria</h3>
        <p>Si la guía automática no es suficiente, un voluntario te atenderá de forma personalizada.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .steps-section {
    padding: 3rem 0 5rem 0;
    background-color: var(--color-bg-light);
  }
  .steps-intro {
    text-align: center;
    margin-bottom: 4rem;
  }
  .steps-intro h2 {
    font-size: var(--font-size-xl);
    margin-bottom: 1rem;
  }
  .steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  .step-card {
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 2.5rem;
    position: relative;
    transition: transform 0.2s ease;
  }
  .step-card:hover {
    transform: translateY(-4px);
  }
  .step-number {
    font-size: var(--font-size-lg);
    font-weight: 800;
    color: var(--color-brand);
    margin-bottom: 1rem;
  }
  .step-card h3 {
    font-size: var(--font-size-lg);
    margin-bottom: 0.75rem;
  }
  .step-card p {
    font-size: var(--font-size-md);
    color: var(--color-text-muted);
  }
  @media (min-width: 768px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>